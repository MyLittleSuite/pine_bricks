import 'package:{{package_name}}/repositories/{{file_name}}_repository.dart';

/// Test case for the class {{name}}RepositoryImpl
void main() {
    late {{name}}Repository service;

    setUp(() {
        service = {{name}}RepositoryImpl();
    });
}